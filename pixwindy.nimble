version     = "0.0.0"
author      = "FolxTeam"
description = "Window creating library for software rendering"
license     = "MIT"

srcDir = "src"

requires "nim >= 1.4.8"
requires "vmath >= 1.1.0"
requires "pixie >= 3.1"
